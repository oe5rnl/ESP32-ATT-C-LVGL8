[env:pico]
platform = raspberrypi
board = pico
framework = arduino

; Upload-Methode für BOOTSEL-Modus (USB-Massenspeicher)
upload_protocol = mbed

; Serial Monitor Baudrate
monitor_speed = 115200

; Build flags
build_flags = 
    -D PICO_BUILD

; Libraries
lib_deps = 
    Wire
